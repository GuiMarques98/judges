#include <bits/stdc++.h>
using namespace std;
#define debug(x) cerr << #x << " = " << (x) << endl
#define IOS ios::sync_with_stdio(false); cin.tie(NULL)
// Greate commom divisor
#define gcd(a, b) __gcd(a, b) 

int main(int argc, char const *argv[])
{
    IOS;
    
    return 0;
}
